# Elevate-Lab
The initial dataset contained duplicate entries, inconsistent date formats, and missing values in the  column. I used Excel to 'Remove Duplicates' feature  and the DATE function to standardize date formats and also gives datatype to the respective column . These cleaning steps improved the accuracy, consistency, and completeness of the data.
